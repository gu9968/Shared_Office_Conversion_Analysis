# Models

최종 XGBoost 예측 모델 및 배포에 필요한 보조 자산입니다.

- `best_tuned_model.pkl` : 최종 XGBoost Classifier
- `best_tuned_threshold.pkl` : 운영 Threshold (0.35)
- `feature_cols.json` : 모델 입력 42개 피처와 순서
- `feature_medians.json` : 결측치 대체용 학습 데이터 중앙값
- `model_metrics.json` : 최종 모델 평가 지표
